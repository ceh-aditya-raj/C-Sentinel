# C-Sentinel
C-Sentinel, a static C code analyzer that tokenizes source code, constructs an Abstract Syntax Tree (AST), and detects stack and heap buffer overflow vulnerabilities with severity scoring, exposed through a FastAPI-based web application.
